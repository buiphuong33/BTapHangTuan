#include <bits/stdc++.h>
using namespace std;
int main() {
    /*int d, m, y;
    cin >> d >> m >> y;
    int k;
    do {
        cin >> k;
        cout << k << endl;
    if(k==0) break;
    } while(k!=0);
    */
    int i = -3;
           	for( ; ; ) {
	                	cout<<"Hello, world!\n";
                    	i ++;
	                	if (i == 0) break;
           	} ;










}
